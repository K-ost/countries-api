const NotFound = (): JSX.Element => {
  return (
    <div className="error">
      There are no countries matching your filter parameters. Try to change your
      parameters.
    </div>
  );
};

export default NotFound;
